
	<main>
		<h1>USUARIOS</h1>
		<!--
		<table border=1>
			<thead>
				<tr>
					<th>username</th>
					<th>email</th>
					<th>name</th>
					<th>Admin?</th>
					<th>&nbsp;</th>
				</tr>
			</thead>
			<tbody>
				<% users.forEach(user => { %>

				<tr>
					<td><%= user.username %></td>
					<td><%= user.email %></td>
					<td><%= user.name %></td>
					<td><% if(user.id_rol == 1){%>SI<%}else{%>NO<%}%></td>
					<td><a href="<%= route %>/../../../users/edit/<%= user.id %>"><%= user.id %></a></td>
				</tr>

				<% }); %>
			</tbody>
		</table>
		-->
		<table class="table" align="center">
		  <thead>
		    <tr>
		      <th scope="col">#</th>
		      <th scope="col">Nombre de Usuario</th>
		      <th scope="col">Email</th>
		      <th scope="col">Nombre y Apellido</th>
		      <th scope="col">Admin?</th>
		      <th scope="col">&nbsp;</th>
		    </tr>
		  </thead>
		  <tbody class="table-group-divider">
			<% 
			let i = 0
			users.forEach(user => { 
				i++
			%>
			<tr>
				<td><%= i %></td>
				<td><%= user.username %></td>
				<td><%= user.email %></td>
				<td><%= user.name %></td>
				<td><% if(user.id_rol == 1){%>SI<%}else{%>NO<%}%></td>
				<td>
					<button type="button" class="btn btn-secondary" onclick="document.location='<%= route %>/../../../users/edit/<%= user.id %>'" >Editar</button>
					<!--<a href="<%= route %>/../../../users/edit/<%= user.id %>"><%= user.id %></a>-->
				</td>
			</tr>

			<% }); %>
		  </tbody>
		</table>
	</main>
	
